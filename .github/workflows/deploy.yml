name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      # Thêm các bước triển khai của bạn ở đây
      - name: Deploy to production
        run: echo "Deploying..."

